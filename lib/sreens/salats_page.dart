import 'package:flutter/material.dart';

class SalatsPage extends StatelessWidget {
  const SalatsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Salats"),
      ),
    );
  }
}
